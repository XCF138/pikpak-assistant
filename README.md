# PIKPAK助手（油猴脚本）

PIKPAK助手（油猴脚本）：把常用的 PikPak 网盘整理操作集中到一个横屏、可拖动、可全屏的悬浮工作台里。直接使用网页登录状态，无需配置账号密码。

## 功能

- **📋 批量复制 / 移动**：把一个文件（或多个文件/文件夹）批量复制或移动到多个目标文件夹，支持全选/反选、按路径自动创建文件夹。
- **🗂 文件整理**：移到回收站（可恢复）、批量解压（zip/rar/7z 到所在目录）。
- **🔎 文件查重**：按「哈希+大小」分组找出重复文件，支持关键字搜索筛选，每组保留时间最新的一个、其余一键移入回收站。
- **🌳 目录树导出**：把文件夹结构导出为 TXT 或 PNG 图片，可自定义扫描深度与格式。
- **✏️ 批量重命名**：按括号、关键字、位置（某字前/后/之间）删除或替换文字，可选加序号 `1. 2. 3.`，预览确认后执行。

## 安装

### 方式一：一键安装（推荐）

1. 安装浏览器扩展 [Tampermonkey](https://www.tampermonkey.net/)。
2. 打开下面这个链接，Tampermonkey 会自动弹出安装确认：

   👉 https://raw.githubusercontent.com/XCF138/pikpak-assistant/main/pikpak-batch-copy.user.js

3. 打开并登录 https://mypikpak.com/ ，页面右下角会出现「PikPak 助手」悬浮球。

> 这样安装后，脚本会自动检查更新（`@updateURL` 已指向本仓库）。

### 方式二：手动安装

1. 打开 [pikpak-batch-copy.user.js](./pikpak-batch-copy.user.js)，复制全部内容。
2. Tampermonkey 里新建脚本，粘贴并保存。

## 使用说明

- 点击悬浮球展开工作台，顶部标签切换功能。
- 按住顶部标题栏可拖动窗口，右下角手柄可缩放；右上角「⛶ 全屏」可切换全屏/窗口，位置大小自动记忆。
- 所有操作都直接调用 PikPak 官方接口，复用你网页的登录态。

## 更新日志

### v1.28.3（2026-09-04）
- **优化**：去掉「我的分享」导出时对文件夹的递归大小统计——大文件夹会一直转圈卡很久。改为：
  - 文件且 API 直接给 `file_size` → 显示真实大小；
  - 否则（尤其是文件夹）直接用 API 返回的 `file_num` 显示「N 个文件」作为第三列，零成本、不扫描。
- 导出行格式变为 `名字--链接--大小/文件数`。

### v1.28.2（2026-09-04）
- **修复**：文件夹分享的大小仍为空。分享项 `file_size` 是字符串 `"0"`、`file_kind` 为 `drive#folder` 时，旧逻辑直接返回了 0，没有递归统计内容。
- `computeShareTotalSize` 现在只在「非文件夹且自带有效 size」时直接返回；文件夹一律用 `file_id` 走 `sumDriveFolderSize` 递归统计真实大小。

### v1.28.1（2026-09-04）
- **修复**：「我的分享」导出大小仍为空。分享项真实字段为 `title`（名字）、`file_size`（大小）、`file_kind`（类型，如 `drive#folder`）、`file_id`（文件/文件夹 id），没有 `name/kind/size` 那些别名。
- `isShareItemFolder` 增加 `file_kind === 'drive#folder'` 判定。
- `computeShareTotalSize` 增加兜底：当分享项只有 `file_id` 而无 `file_list` 时，文件夹用 `file_id` 走 `sumDriveFolderSize` 递归统计，文件用 `getFileInfo(file_id)` 取真实大小。
- 诊断日志补全 `file_id / file_size / file_kind / file_num`。

### v1.28.0（2026-09-04）
- **重构**：把「导出我的分享」合并到「目录树」面板。目录树面板顶部新增「来源」下拉：
  - **我的网盘**：保持原有目录树导出（勾选文件夹 → TXT / PNG）。
  - **我的分享**：加载 `/drive/v1/share/list`，列出所有分享项并支持勾选、筛选、全选/反选；点「导出选中分享」输出 txt，每行格式仍为 `名字--链接--总大小`。
- **移除**：左侧独立的青绿色「导出分享」侧边栏入口。
- **修复**：导出成功但名字/大小/链接为空的问题。新增字段别名探测：名字 `name/file_name/title/share_name/subject`、链接 `share_url/url/link/share_link/web_link`、大小 `size/total_size/share_size/file_size`，并会按 `share_id/id` 兜底拼接分享链接。
- **诊断**：分享项加载成功后，F12 控制台会自动打印「分享项示例字段」与脱敏摘要，便于继续定位字段名。

### v1.27.4（2026-09-04）
- **修复**：导出分享「请求成功但解析出 0 条」。v1.27.3 已确认接口 `/drive/v1/share/list` 能成功返回（含分页 `page_token`），但旧代码 hardcode 取 `resp.shares / share_list / items / files` 都取不到数组。
- **新增 `extractShareItems()`**：依次探测 `shares / share_list / list / items / files / share / data.shares / data.share_list / data.list / data.items / data 数组` 等多个候选字段，取第一个真正的数组；翻页循环也改用它。
- **新增诊断输出**：当本页解析到 0 条时，控制台会打印「分享列表响应字段：…→ 本页解析到 N 条」，并额外打印「顶层字段 = 键:类型(数组长度) | data 字段 = …」结构摘要 + 「分享列表原始响应（前1500字符）」。把这几行贴回来即可锁定真实字段名。
- 重装后请点一次「导出分享」，把控制台里「分享列表响应字段：…→ 本页解析到 N 条」以及（若 N=0）「分享列表原始响应（前800字符）：…」这两行发我，即可把字段名写死。

### v1.27.3（2026-09-04）
- **确认正确接口**：用户 Console 显示官方「我的分享」页用的是 `GET /drive/v1/share/list?limit=100&thumbnail_size=SIZE_LARGE...`。v1.27.3 把 `/share/list` 作为首选路径（不再依赖捕获）。
- **修复模板污染**：v1.27.2 的钩子把我们自己降级重试的 400 请求也存成了「官方模板」，导致好模板被坏模板覆盖。现在只在响应成功（`res.ok`）时才保存模板。
- 重装后无需先点「我的分享」页面即可直接导出（捕获模板仅作为官方参数变化时的保险）。

### v1.27.2（2026-09-04）
- **修复**：导出分享仍报「请求参数错误」——`GET /drive/v1/share` 实为「按 share_id 查分享详情」接口，不带 share_id 必 400，它根本不是「我的分享」列表接口。
- **改用抓取官方请求**：脚本 fetch 钩子现在会记录用户在官方网页打开「我的分享」页面时发出的真实列表请求（存 localStorage），导出时优先回放该模板（自动翻页）；无模板时才降级尝试常见参数组合。
- **使用方法**：重装后先在 PikPak 左侧点开一次「我的分享」页面，再点「导出分享」即可（控制台会打印「已捕获我的分享请求模板」）。

### v1.27.1（2026-09-04）
- **修复**：导出分享报「请求参数错误」。原请求带了 `with_audit=true`（那是 files 接口的参数，share 接口不接受）。改为多组参数依次降级重试（`share_channel=ALL` → 带 thumbnail_size → 仅 limit → parent_id=0 → 无参数），取第一个真正拿到数据的组合；请求成功的响应字段与失败原因都打到 F12 控制台，便于定位。

### v1.27.0（2026-09-04）
- **新增**：「导出分享」侧边栏入口（青绿色胶囊，位于 PIKPAK 助手入口下方）。点击后自动拉取「我的分享」全量列表并导出为 txt，每行格式：`文件名--分享链接--总大小`。
  - 单文件分享直接取文件大小；文件夹分享自动递归统计自己网盘里对应文件夹的总大小（带缓存与深度/数量上限，防止大目录卡死）。
  - 导出过程右下角实时显示进度；txt 带 BOM，记事本打开不乱码；文件名形如 `我的分享_20260904.txt`。

### v1.26.2（2026-08-27）
- **修复**：查重「缩略图相似」几乎不生效。根因：原实现用 canvas 计算感知哈希(aHash) 需要在 `<canvas>` 读取缩略图像素，但 PikPak 缩略图多为跨域 CDN 且无 CORS 头，导致 canvas 被污染、`getImageData` 抛错、哈希恒为 null，分组永远为空。
  - 去掉 `img.crossOrigin` 设置，并在 `groupByThumbnail` 增加「相同缩略图 URL」兜底分组：跨域无法读像素时，按缩略图 URL 相同判定为同一图（至少兜住内容哈希缺失的精确重复）。
  - aHash 近相似仅在同源/CDN 允许跨域读像素时生效；UI 文案同步说明该取舍。

### v1.26.1（2026-08-27）
- **修复**：查重结果中单个文件 checkbox 无法单独勾选。根因是文件行 `<div>` 也带 `data-fi` 属性，`bindDupResultEvents` 用 `[data-fi]` 选择器时把 div 也绑上了 change 监听器，div 的 `checked` 为 undefined，覆盖/干扰了 input 的勾选状态。已限定选择器为 `input[type="checkbox"][data-fi]`。

### v1.26.0（2026-08-27）

- **修复查重勾选两大 bug**：
  1. **筛选后无法选择删除哪个**：`renderDupGroups()` 过滤时把「过滤后数组下标」当成 `state.dupGroups` 的原始下标去取勾选状态，过滤后两者对不上，导致筛选状态下点 A 组的框实际改了 B 组、没法正确选中要删的文件。现改为渲染时保留原始 `gi` 下标（只隐藏不匹配的组，不重排），所有勾选/删除逻辑统一按原始下标映射，筛选状态下选哪个就删哪个。
  2. **勾一个却显示十几个被删**：`_dupChecked` 默认是 `undefined`，渲染用 `!== false` 把它当「已勾选」显示（一堆框看着是勾上的），但删除逻辑 `if (f._dupChecked)` 把 `undefined` 当「未勾选」，显示和实际不一致，用户根本不知道会删几个。现扫描完成后显式把每个文件 `_dupChecked = false`（默认不勾，手动选），显示与删除完全一致——勾哪个删哪个。
- 查重结果默认**不预勾选**；需要批量删可点「组头」选整组，或用「全选可删除项」。若你更想要「默认保留每组最新一个、其余勾选删除」的旧行为，告诉我即可切回。

### v1.25.9（2026-08-26）

- **优化查重设置区排版**：把「算法」勾选、「结果操作按钮」、「文件类型 / 相似阈值 / 递归扫描」三层视觉分离，避免挤在同一行导致的错位/换行凌乱。
  - 算法独占一行，勾选框自然换行；操作按钮（全选可删除项 / 反选 / 移到回收站）放在算法行下方单独一行并右对齐，与勾选区域对齐。
  - 文件类型、相似阈值、递归扫描子文件夹放在同一行，递归标签禁止换行，避免之前被挤成多段的字样。
  - 新增 `.pp-dup-rules` / `.pp-dup-algo-row` / `.pp-dup-action-row` / `.pp-dup-filter-row` 等样式类，取消操作按钮在结果操作区的 `margin-top`，整体更紧凑整齐。

### v1.25.8（2026-08-26）

- **修复入口/图标整段不显示的根因（关键）**：v1.25.2 把查重操作按钮移到算法行时，在 `updateDupActionState()` 和 `bindEvents()` 里直接调用了 `shadowRoot.getElementById(...)`，但 `shadowRoot` 只是 `init()` 内部的局部 `const`，这两个函数访问不到它，于是运行到 `bindEvents()` 时抛 `shadowRoot is not defined`，导致 `init()` 后续代码（含左侧入口注入 `startSidebarInjection`）整体中断不执行——这正是 v1.25.2 之后入口/图标消失的真凶。已统一改为 `ui.shadowRoot.getElementById(...)`（与 `$()` 助手一致）。v1.25.1 没有这段代码所以正常。
- 仍以用户确认正常的 **v1.25.1** 为基线 + v1.25.5 的按钮右侧布局 + 图标兜底，`@version` 升到 1.25.8。

### v1.25.7（2026-08-26）

- **修复左侧入口图标空白（改用 v1.25.1 已验证写法）**：v1.25.6 把 `buildEntryContent()` 改成 `createElement('img')` + `error` 监听器的写法，在你的环境里图标不显示（仅 v1.25.1 的 favicon 写法正常）。现改回 v1.25.1 的写法——favicon 直接写进 `innerHTML`（你环境里能正常显示），再额外用 JS 绑定 `error` 事件做 SVG 兜底（加载失败才回退），并补判 `img.complete && naturalWidth===0` 以防 error 在监听器绑定前已同步触发。
- 以用户确认正常的 **v1.25.1** 为基线（左侧入口正常）+ v1.25.5 的按钮右侧布局 + 图标兜底，`@version` 升到 1.25.7。

### v1.25.6（2026-08-26）

- **修复左侧入口图标空白（回退 v1.25.3 的兜底）**：v1.25.2/1.25.5 基线用 `<img>` 直接加载 PikPak favicon，favicon 取不到/404 时图标就空白。现把 v1.25.3 已验证的兜底补回 `buildEntryContent()`：`<img>` 监听 `error` 事件，加载失败时回退到内置白色云盘 SVG 图标，保证入口一定有图标。`getPikpakIconUrl()` 包 try/catch。**未引入 v1.25.4 的 Shadow DOM 注入逻辑**，避免入口整段消失。
- 以用户确认正常的 **v1.25.1** 为基线（左侧入口正常）+ v1.25.5 的按钮右侧布局，`@version` 升到 1.25.6。

### v1.25.5（2026-08-26）

- **查重操作按钮移到算法行右侧独立列**：早期按钮在吸底条、v1.25.2 曾放到「算法」行最右侧但易挤到上方；现改为左侧放算法勾选框（精准匹配/视频时长相似/名称相似/缩略图相似），右侧独立一列放「全选可删除项 / 反选 / 将勾选项移到回收站」三个操作按钮，左右并排、不再占用顶部空间。
- 按钮仍为 `pp-btn-sm` 小尺寸；无查重结果时自动禁用（有结果才可用）。
- 以用户确认正常的 **v1.25.1** 为基线，左侧入口（蓝色背景 + 网页 favicon 图标）保持正常，`@version` 升到 1.25.5。

### v1.25.2（2026-08-26）

- **查重操作按钮移出吸底条、并入算法行**：之前吸底条与结果列表之间有缝隙/对不齐；现把「全选可删除项 / 反选 / 将勾选项移到回收站」三个按钮移到「算法」设置行的最右侧（`margin-left:auto` 右对齐），常驻显示、不再随结果滚动，也不必滑到最下面。
- 按钮改为 `pp-btn-sm` 小尺寸；无查重结果时自动禁用（有结果才可用）。
- 移除原 `.pp-dup-actions` 的 sticky 样式与渲染期绑定，改在 `bindEvents` 中一次性绑定。

### v1.25.1（2026-08-26）

- **修复查重结果缩略图不显示**：之前渲染缩略图时给 `<img>` 加了 `crossorigin="anonymous"`，导致无 CORS 头的缩略图被浏览器拦截；已移除 display `<img>` 的 crossorigin，仅保留 canvas 哈希计算时的 crossorigin。
- **扩大缩略图字段兼容**：`collectDupItems()` 现在尝试从 `thumbnail / thumbnail_link / image.thumbnail_link / image.small_thumbnail_link / links.thumbnail.url / links.icon.url / media_info.thumbnail.url / media_info.thumbnail_url / media_info.video_metadata.thumbnail.url / params.thumbnail / params.thumbnail_url / params.thumbnail_link / params.icon_link / icon_link` 等字段提取缩略图。
- **优化查重操作条样式**：吸底条改为 `width:100%`、增加底部阴影、调整 padding/margin，避免内容遮挡、视觉更对齐。
- 缩略图加载失败时通过 JS 监听 `error` 事件回退为文件类型 emoji。

### v1.25.0（2026-08-26）

- **查重结果支持真实缩略图**：优先显示 PikPak 返回的文件/视频封面缩略图，加载失败或没有缩略图时回退到文件类型图标。
- **新增「缩略图相似」查重算法**（默认关闭）：对图片/视频封面计算 aHash 感知哈希，Hamming 距离 <= 阈值归为一组；文件多时较慢，且依赖跨域缩略图可加载。可通过复选框开启/关闭。
- **查重操作按钮改为吸底**：「全选可删除项」「反选」「将勾选项移到回收站」三个按钮现在会吸在查重结果面板底部，滚动列表时始终可见，不用滑到最下面。

### v1.24.0（2026-08-26）

- **文件查重结果改为分组卡片格式**：每组显示「X 项 | 总大小 文件名」头部，下面列出每个文件的勾选框、文件类型图标/缩略图、文件名、所在路径、大小、时长、修改日期，风格贴近 PikPak 官方文件列表。
- **查重支持手动勾选删除**：默认保留每组时间最新的一个，其余默认勾选为「待删除」；用户可以单独勾选/取消任意文件，也可以按组全选、全选可删除项、反选。
- **新增「文件类型」筛选**：可选择「全部文件 / 仅视频 / 仅图片」进行查重。
- **标题栏「检查更新」按钮旁新增版本号**：显示当前脚本版本（如 `v1.24.0`），自动与 `@version` 同步。
- 新增 `fmtDuration()` 时长格式化辅助函数。

### v1.23.1（2026-08-26）

- **给油猴脚本列表添加图标**：在元数据块中新增 `@icon` / `@iconURL`，指向 PikPak 官方 favicon (`https://mypikpak.com/favicon.ico`)，Tampermonkey 脚本管理界面会显示该图标。

### v1.23.0（2026-08-26）

- **左侧入口改成蓝色背景胶囊**，白色文字，hover 加深，更醒目。
- **入口左侧图标改为 PikPak 网页 favicon**：优先读取页面 `<link rel="icon">` 的 `href`，读不到则回退到当前域名的 `/favicon.ico`；用 `<img>` 渲染成 18×18 图标，替换原来的字母 "P"。
- 保持行高 44px、padding/gap 与官方项对齐。

### v1.22.2（2026-08-26）

- **入口彻底消失问题的修复**：不再硬依赖 `.wp-s-aside-nav__main-top` 这个 class。改为**按文字定位官方项**（如「已加星标」「全部文件」「最近保存」等），找到后插入到「已加星标」后面；若找不到文字项，才回退到旧的 class 选择器兜底。这样即使 PikPak 改了 class 名，入口仍能注入。
- 同时加入 `MutationObserver` 兜底监听，侧边栏如果是异步渲染也能被捕获；注入成功/失败都会打 `console.log`，方便你反馈时直接告诉我日志内容。
- 在 `init()` 开头增加版本日志，方便确认 Tampermonkey 里跑的确实是新版。

### v1.22.1（2026-08-26）

- **彻底杜绝「白卡片」**：给左侧入口的 `.pp-quick-entry / .pp-quick-entry-a / .pp-quick-entry-ico` 强制 `background-color: transparent !important; border:none !important; box-shadow:none !important;`，用 `!important` 压过 PikPak 页面可能给侧边栏子元素套上的任何背景/边框/阴影，确保入口永远与官方项一样透明融合。hover/active 的高亮也同步用 `!important` 保留。
- 同时确认右键里不再有任何会渲染成白卡片的老代码（`.pp-fab` 浮动球、`.pp-quick-fallback-left` 等均已在 1.20–1.22 移除）。

### v1.22.0（2026-08-26）

- **左侧入口彻底改成参考脚本同款逻辑**：直接 append 一个普通 `<div>` 到官方侧边栏容器 `.wp-s-aside-nav__main-top` 底部；删掉会留下白底卡片的「左侧固定兜底」方案，入口只应该出现在侧边栏里，透明背景、44px 行高、与官方项完全并排。
- 去掉 `.pp-quick-entry-li` / `.pp-quick-fallback-left` 残留样式。

### v1.21.0（2026-08-26）

- **删除批量复制头顶的步骤条**（选源 / 选目标 / 执行）。
- **统一各功能页间距**：`.pp-body` 统一加 `padding: 10px 12px 12px; gap: 8px`，文件整理 / 查重 / 目录树 / 重命名页面与批量复制页面空隙一致。

### v1.20.0（2026-08-26）

- **彻底删除脚本内置的右下角浮动球 `.pp-fab`**（之前被我遗忘，导致右下角一直有一个蓝色「PIKPAK助手」按钮）。从 HTML、CSS、事件绑定、面板开关逻辑中一并移除。
- 左侧入口样式改成**严格对齐官方侧边栏项**：透明背景、20px 图标与官方图标对齐、文字仅显示「PIKPAK」、hover 用官方浅灰 `#f5f6fa`。

### v1.19.0（2026-08-26）

- 入口形态改为**官方侧边栏项模式**：左边一个小图标（蓝色方块「P」），右边是字「PIKPAK助手」（水平排列）。
- **修复右下角入口删不掉的问题**：旧版脚本因 `@name` 改名被 Tampermonkey 当成另一个脚本仍在运行（右下角 FAB 残留）。本版改用唯一 id `pikpak-assistant-entry`，并在初始化时主动清除旧版残留的 `.pp-quick-fab` / `pp-quick-entries-wrap` 元素（含 15s 观察者兜底）。
- **入口秒出**：若官方侧边栏 `.wp-s-aside-nav__main-top` 还没渲染，立即在屏幕最左侧显示一个固定入口，随后自动移入侧边栏，不再干等十几秒。

### v1.18.0（2026-08-26）

- 入口改为**完全仿照参考脚本「网盘文件批量重命名」的做法**：注入到 PikPak 左侧官方侧边栏容器 `.wp-s-aside-nav__main-top`，与官方项**完全并排**（蓝色图标 + 蓝色文字，58px 宽，垂直居中）。
- **彻底移除右下角浮动图标**。改为轮询等待侧边栏渲染后注入一次；若侧边栏始终未出现，才降级为屏幕最左侧固定入口（非右下角）。

### v1.17.0（2026-08-26）

- 蓝色"➕ PIKPAK助手"入口按钮移到**左侧**：优先注入 PikPak 官方侧边栏底部，找不到侧边栏则固定屏幕左侧。不再占用右下角。

### v1.16.0（2026-08-26）

- 入口回退为单个蓝色 FAB pill 按钮"➕ PIKPAK助手"（仿官方新建按钮风格），固定在 PikPak 网页右下角，点击展开/收起助手悬浮窗。

### v1.15.0（2026-08-26）

- **入口从右下角 FAB 改为注入 PikPak 官方侧边栏底部**（仿参考脚本「网盘文件批量重命名」的 enter-component 风格），与官方"重命名"等项并排。找不到侧边栏时降级为屏幕左侧浮动入口。
- 入口项：📋 批量复制/移动、🗂 文件整理、🔎 文件查重、🌳 目录树、✏️ 批量重命名。

### v1.14.0（2026-08-26）

- 脚本名改为「PIKPAK助手」（全大写，匹配你截图样式）。
- **侧边栏快捷入口改成 FAB 按钮**：注入 PikPak 网页的单个蓝色 pill 按钮 "➕ PIKPAK助手"（仿你截图样式，右下角浮动，点击展开/收起助手悬浮窗）。

### v1.13.0（2026-08-26）

- 步骤条文字缩短（"选文件"→"选源"、"选目标文件夹"→"选目标"），解决步骤条文字超出边框问题。
- **新增 PikPak 官方侧边栏快捷入口**：自动检测 PikPak 网页侧边栏，在其下方注入"PikPak 助手"区块，按"图标+文字"垂直排列（仿官方风格），含 5 个入口（批量复制/移动、文件整理、文件查重、目录树、批量重命名）。点击自动展开悬浮窗并切到对应 tab，找不到官方侧边栏时降级为屏幕左侧浮动入口条。

### v1.12.0（2026-08-26）

- 文件查重 tab 调整为「浏览/列表在上、规则+操作在下」布局（参考重命名 tab）。
- **新增脚本更新检查**：
  - 启动后延迟 3 秒自动检查 GitHub 远端版本
  - 头部「🔄 检查更新」按钮可手动检查
  - 有新版本时自动弹窗（含当前/最新版本对比 + 立即更新/稍后按钮）
  - 标题栏右侧动态显示红色脉冲徽章 `🆕 vX.Y.Z`（点它随时打开更新弹窗）
  - 检查函数走 GitHub raw URL，零外部依赖

### v1.11.0（2026-08-26）

- 文件查重新增「📂 选目录」模式：可浏览子文件夹并多选要扫描的文件夹，顶部已选徽章 + 单个移除/清空。扫描时只查勾选的文件夹（不递归），未勾选时仍按默认范围扫描。

### v1.10.0（2026-08-26）

- 头部副标题简化为只显示 `by XCF138`（加粗大号，更显眼）。
- 文件查重升级为多算法模式，**套用 PikPak 增强大师的文件分析算法**：
  - 精准匹配（哈希+大小）
  - 视频时长相似（相近时长+相近大小）
  - 名称相似（清理后文件名+大小）
- 查重支持**递归子文件夹**（可勾选），解决之前只能扫当前层的问题。
- 新增相似度阈值（宽松/标准/严格）和算法开关（可单独启用/禁用某算法）。
- 文件名清理函数（`cleanNameForDup`）单测 10/10 通过。

### v1.9.0（2026-08-26）

- 面板改为横屏布局（更宽），右上角新增「全屏/窗口」切换按钮。
- 文件查重从「文件整理」独立成单独一栏，新增关键字搜索筛选重复组。
- 文件整理仅保留「移到回收站」「批量解压」两项操作。
- 头部新增作者名与 GitHub 链接。
- 新增 `@updateURL` / `@downloadURL`，支持从 GitHub 一键安装与自动更新。

### v1.8.2（2026-08-26）

- 复制模式下文件夹也支持勾选框，可多选文件夹一起复制/移动。

### v1.8.0（2026-08-26）

- 新增批量移动（复制 tab 内切换）。
- 新增文件整理（回收站/解压/查重）。

## 说明

- 删除操作默认走「回收站」，可恢复，不会彻底删除文件。
- 仅供个人网盘文件整理使用，请遵守 PikPak 服务条款。

## 许可证

[MIT](./LICENSE) © XCF138
